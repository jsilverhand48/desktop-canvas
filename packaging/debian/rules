#!/usr/bin/make -f
# Reference debian/rules for desktop-canvas.
%:
	dh $@ --buildsystem=cmake+ninja

override_dh_auto_configure:
	dh_auto_configure -- \
		-DCMAKE_BUILD_TYPE=Release \
		-DDESKTOP_CANVAS_TESTS=OFF
