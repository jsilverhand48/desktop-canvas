// AudioPolicy is header only today; this translation unit exists so the
// build target has a stable place for future PipeWire integration (e.g.
// automatic pause when another app plays audio).
#include "audio/AudioPolicy.h"
